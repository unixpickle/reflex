# Compute the sum from N=1 to i
IntSum = {
  i = 0
  sum = 0
  result = i.select(
    true = @(
      i := i.sub(y=1)!
      sum := sum.add(y=i)!
    )!
    false = sum 
  )!
}

result = IntSum(i=10000)!
